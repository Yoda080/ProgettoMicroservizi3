# -----------------------------------------------------------
# FASE 1: BUILD (Compilazione del codice) - USA NET 8 STABILE
# -----------------------------------------------------------
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src

# Copia l'intera cartella del microservizio
COPY MovieCatalogService/ ./MovieCatalogService/ 

# Vai nella directory del progetto all'interno del container
WORKDIR /src/MovieCatalogService

# Rimuoviamo la riga "RUN dotnet restore..."
# Consolidiamo: Esegui solo 'dotnet publish'. Rimuovendo '--no-restore', 
# forziamo il download completo e la risoluzione di tutte le dipendenze.
RUN dotnet publish "MovieCatalogService.csproj" -c Release -o /app/publish

# -----------------------------------------------------------
# FASE 2: FINALIZZAZIONE (Immagine di Runtime leggera) - USA NET 8 STABILE
# -----------------------------------------------------------
FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS final
WORKDIR /app

# Copia i file compilati
COPY --from=build /app/publish .

# Espone la porta corretta: 5001
EXPOSE 5002

# Comando di avvio
ENTRYPOINT ["dotnet", "MovieCatalogService.dll"]